'use client';import {useState} from 'react';import Link from 'next/link';import {ensureWorkspace,login} from '@/lib/api';import Nav from '@/components/Nav';
export default function Login(){const [email,setEmail]=useState('demo@ascend.test'),[password,setPassword]=useState('Demo12345!'),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{await login(email,password);await ensureWorkspace();setTimeout(()=>{location.href='/dashboard'},260)}catch{setError('No pudimos verificar esos datos. Revisa tu correo y contraseña.')}finally{setBusy(false)}}return <><Nav/><main className="auth-shell"><section className="auth-panel"><div className="auth-card"><div className="eyebrow">Acceso al estudio</div><h1>Bienvenido de nuevo</h1><p className="muted">Continúa proyectos, genera nuevas variantes y administra tu biblioteca visual.</p>{error&&<p className="error" aria-live="polite">{error}</p>}<form onSubmit={submit}><label className="field">Correo<input className="input" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="field">Contraseña<div className="row" style={{gap:8}}><input className="input" style={{flex:1}} type={show?'text':'password'} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn secondary" type="button" onClick={()=>setShow(!show)}>{show?'Ocultar':'Ver'}</button></div></label><div className="row" style={{justifyContent:'space-between',margin:'6px 0 18px'}}><label className="row"><input type="checkbox"/> Recordarme</label><Link className="muted" href="/login">Recuperar contraseña</Link></div><button className="btn" style={{width:'100%'}} disabled={busy}>{busy?'Ingresando...':'Iniciar sesión'}</button></form><p className="muted">¿Nuevo en Ascend? <Link href="/register">Crear cuenta</Link></p></div></section><section className="auth-visual"><blockquote>Una buena idea merece una dirección visual extraordinaria.</blockquote></section></main></>}
